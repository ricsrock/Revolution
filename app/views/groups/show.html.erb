<div id="group">
	<%= render :partial => "#{@group.partial_name}" %>
</div>

<div id="actions">
	<%= render partial: "actions", locals: { group: @group } %>
</div>

<div id="meetings">
	<%= render partial: 'meetings', locals: { group: @group, title: @title } %>
</div>