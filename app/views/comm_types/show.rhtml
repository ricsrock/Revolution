<% for column in CommType.content_columns %>
<p>
  <b><%= column.human_name %>:</b> <%=h @comm_type.send(column.name) %>
</p>
<% end %>

<%= link_to 'Edit', :action => 'edit', :id => @comm_type %> |
<%= link_to 'Back', :action => 'list' %>
