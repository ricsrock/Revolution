<%= hidden_field_tag :new_household_id, @new_household.id %>
<%= submit_tag 'Move Person', class: 'button radius right', confirm: "Are you sure you want to move this person to the #{@new_household.name} household?" %>
