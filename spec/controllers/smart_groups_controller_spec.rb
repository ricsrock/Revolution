require 'spec_helper'

# This spec was generated by rspec-rails when you ran the scaffold generator.
# It demonstrates how one might use RSpec to specify the controller code that
# was generated by Rails when you ran the scaffold generator.
#
# It assumes that the implementation code is generated by the rails scaffold
# generator.  If you are using any extension libraries to generate different
# controller code, this generated spec may or may not pass.
#
# It only uses APIs available in rails and/or rspec-rails.  There are a number
# of tools you can use to make these specs even more expressive, but we're
# sticking to rails and rspec-rails APIs to keep things simple and stable.
#
# Compared to earlier versions of this generator, there is very limited use of
# stubs and message expectations in this spec.  Stubs are only used when there
# is no simpler way to get a handle on the object needed for the example.
# Message expectations are only used when there is no simpler way to specify
# that an instance is receiving a specific message.

describe SmartGroupsController do

  # This should return the minimal set of attributes required to create a valid
  # SmartGroup. As you add validations to SmartGroup, be sure to
  # update the return value of this method accordingly.
  def valid_attributes
    {  }
  end

  # This should return the minimal set of values that should be in the session
  # in order to pass any filters (e.g. authentication) defined in
  # SmartGroupsController. Be sure to keep this updated too.
  def valid_session
    {}
  end

  describe "GET index" do
    it "assigns all smart_groups as @smart_groups" do
      smart_group = SmartGroup.create! valid_attributes
      get :index, {}, valid_session
      assigns(:smart_groups).should eq([smart_group])
    end
  end

  describe "GET show" do
    it "assigns the requested smart_group as @smart_group" do
      smart_group = SmartGroup.create! valid_attributes
      get :show, {:id => smart_group.to_param}, valid_session
      assigns(:smart_group).should eq(smart_group)
    end
  end

  describe "GET new" do
    it "assigns a new smart_group as @smart_group" do
      get :new, {}, valid_session
      assigns(:smart_group).should be_a_new(SmartGroup)
    end
  end

  describe "GET edit" do
    it "assigns the requested smart_group as @smart_group" do
      smart_group = SmartGroup.create! valid_attributes
      get :edit, {:id => smart_group.to_param}, valid_session
      assigns(:smart_group).should eq(smart_group)
    end
  end

  describe "POST create" do
    describe "with valid params" do
      it "creates a new SmartGroup" do
        expect {
          post :create, {:smart_group => valid_attributes}, valid_session
        }.to change(SmartGroup, :count).by(1)
      end

      it "assigns a newly created smart_group as @smart_group" do
        post :create, {:smart_group => valid_attributes}, valid_session
        assigns(:smart_group).should be_a(SmartGroup)
        assigns(:smart_group).should be_persisted
      end

      it "redirects to the created smart_group" do
        post :create, {:smart_group => valid_attributes}, valid_session
        response.should redirect_to(SmartGroup.last)
      end
    end

    describe "with invalid params" do
      it "assigns a newly created but unsaved smart_group as @smart_group" do
        # Trigger the behavior that occurs when invalid params are submitted
        SmartGroup.any_instance.stub(:save).and_return(false)
        post :create, {:smart_group => {  }}, valid_session
        assigns(:smart_group).should be_a_new(SmartGroup)
      end

      it "re-renders the 'new' template" do
        # Trigger the behavior that occurs when invalid params are submitted
        SmartGroup.any_instance.stub(:save).and_return(false)
        post :create, {:smart_group => {  }}, valid_session
        response.should render_template("new")
      end
    end
  end

  describe "PUT update" do
    describe "with valid params" do
      it "updates the requested smart_group" do
        smart_group = SmartGroup.create! valid_attributes
        # Assuming there are no other smart_groups in the database, this
        # specifies that the SmartGroup created on the previous line
        # receives the :update_attributes message with whatever params are
        # submitted in the request.
        SmartGroup.any_instance.should_receive(:update).with({ "these" => "params" })
        put :update, {:id => smart_group.to_param, :smart_group => { "these" => "params" }}, valid_session
      end

      it "assigns the requested smart_group as @smart_group" do
        smart_group = SmartGroup.create! valid_attributes
        put :update, {:id => smart_group.to_param, :smart_group => valid_attributes}, valid_session
        assigns(:smart_group).should eq(smart_group)
      end

      it "redirects to the smart_group" do
        smart_group = SmartGroup.create! valid_attributes
        put :update, {:id => smart_group.to_param, :smart_group => valid_attributes}, valid_session
        response.should redirect_to(smart_group)
      end
    end

    describe "with invalid params" do
      it "assigns the smart_group as @smart_group" do
        smart_group = SmartGroup.create! valid_attributes
        # Trigger the behavior that occurs when invalid params are submitted
        SmartGroup.any_instance.stub(:save).and_return(false)
        put :update, {:id => smart_group.to_param, :smart_group => {  }}, valid_session
        assigns(:smart_group).should eq(smart_group)
      end

      it "re-renders the 'edit' template" do
        smart_group = SmartGroup.create! valid_attributes
        # Trigger the behavior that occurs when invalid params are submitted
        SmartGroup.any_instance.stub(:save).and_return(false)
        put :update, {:id => smart_group.to_param, :smart_group => {  }}, valid_session
        response.should render_template("edit")
      end
    end
  end

  describe "DELETE destroy" do
    it "destroys the requested smart_group" do
      smart_group = SmartGroup.create! valid_attributes
      expect {
        delete :destroy, {:id => smart_group.to_param}, valid_session
      }.to change(SmartGroup, :count).by(-1)
    end

    it "redirects to the smart_groups list" do
      smart_group = SmartGroup.create! valid_attributes
      delete :destroy, {:id => smart_group.to_param}, valid_session
      response.should redirect_to(smart_groups_url)
    end
  end

end
